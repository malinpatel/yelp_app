= form_for @restaurant, html: { multipart: true } do |f|
  = f.label :name
  = f.text_field :name, required: true
  = f.label :description
  = f.text_area :description
  = f.file_field :image
  = f.submit
